import React from 'react';

const StudioSidebar = () => {
  // Gallery-specific sidebar content like categories, tags, or shortcuts
  return (
    <div>
      <h3 className="text-lg font-semibold mb-4 text-white">Studio Filters</h3>
      {/* Example: filters for studios by date, tags, etc */}
      <p className="text-gray-400">Filter or manage your studios here.</p>
    </div>
  );
};

export default StudioSidebar;
